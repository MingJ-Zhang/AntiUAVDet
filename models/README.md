Third-party detector trees live here. Each subdirectory keeps its original
license. The `antiuavdet` CLI dispatches into these trees via PYTHONPATH.

Do not mix Ultralytics forks in one Python environment.
