# Data

- `gt/` — test-set COCO ground truth snapshots (FDD / AntiUAV / DUT-DVE). Required for eval.
- Images live **outside** the repo. Set `ANTIUAVDET_DATA` (see [`docs/datasets.md`](../docs/datasets.md)).
